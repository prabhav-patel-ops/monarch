# Monday — 2026-09-14

## Project task — Options Strategy Lab

**Task:** Connect Angel One data adapter to the prepared Options Strategy Lab scaffold.

**Definition of done:**
- create/use a local `.env` with Angel One credentials; never commit secrets
- wire the authenticated Angel One client into `AngelOneProvider`
- map one option-chain response into the normalized `OptionQuote` model
- run the strategy test suite for straddle, strangle, strip and strap
- save one reproducible sample/fixture or normalization test without secrets/tokens

**Timebox:** 30–45 minutes for the first integration pass.

**Why tomorrow:** this is the next user-owned step after the team bootstrap; the broker credentials/API access intentionally stay with Prabhav.

**Team handoff:** Jasmine scopes/reports; Rose handles strategy/data assumptions; Lily owns integration structure; Kitty reviews normalization, tests and secret hygiene.

> Note: Monarch's live quest board is stored in `monarch.v1` in browser localStorage. This repository task is the durable plan for 2026-09-14; GitHub cannot directly write into the phone's existing localStorage save.